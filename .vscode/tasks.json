x{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Local Server",
            "type": "shell",
            "command": "python -m http.server 8080",
            "isBackground": true,
            "problemMatcher": {
                "pattern": {
                    "regexp": "^Serving HTTP on :: port 8080"
                },
                "background": {
                    "activeOnStart": true,
                    "beginsPattern": "^Serving HTTP on",
                    "endsPattern": "^Serving HTTP on"
                }
            },
            5
            "presentation": {
                "reveal": "silent"
            }
        }
    ]
}